{
 "baseline t16": [
  {
   "decode_tps": 170.80371450633578,
   "prefill_tps": 2981.954470300867,
   "free_mb": 1025,
   "texts": [
    "A **hash map** (also known as a **hash table**) is a data structure that stores key-value pairs and uses a **hash function** to compute an index (or \"bucket\") where the key-value pair should be stored. However, since the hash function maps keys to a finite number of indices, **collisions**\u2014where two different keys hash to the same index\u2014are inevitable.\n\nTo handle collisions, hash maps use various strategies. The two most common methods are:\n\n---\n\n### 1. **Chaining (Separate Chaining)**\n\n- **How it works**:  \n  Each bucket (index) in the hash map is a **linked list** (or another data structure like a tree or array) that stores all key-value pairs that hash to that index.\n\n- **Process**:\n  1. When inserting a key-value pair, the hash function computes the bucket index.\n  2. If the bucket is empty,",
    "Here's a Python class implementing a **Least Recently Used (LRU) Cache** with `get` and `put` methods:\n\n```python\nclass LRUCache:\n    def __init__(self, capacity: int):\n        \"\"\"\n        Initialize the LRU cache with a given capacity.\n        \n        Args:\n            capacity (int): Maximum number of items the cache can hold\n        \"\"\"\n        self.capacity = capacity\n        self.cache = {}  # Dictionary to store key-value pairs\n        self.order = []  # List to maintain access order (most recent at end)\n\n    def get(self, key: int) -> int:\n        \"\"\"\n        Retrieve a value from the cache by key.\n        \n        Args:\n            key (int): The key to look up\n            \n        Returns:\n            int: The value if key exists, otherwise -1\n        \"\"\"\n        if key in self.cache:\n            # Move the accessed key to the end (most",
    "The French Revolution was driven by a combination of long-standing social, economic, and political tensions. France was deeply divided by a rigid social structure known as the Ancien R\u00e9gime, which divided society into three estates: the clergy, the nobility, and the common people (the Third Estate). The Third Estate, which included peasants, workers, and the bourgeoisie, bore the brunt of taxation while having little political power, fueling widespread resentment.\n\nEconomically, France faced a severe financial crisis due to years of costly wars, including support for the American Revolution, extravagant spending by King Louis XVI, and an inefficient tax system. The government was unable to balance its budget, leading to massive debt. Attempts to reform taxation, such as calling the Estates-General in 1789, failed to resolve the crisis, as the Third Estate demanded equal representation, highlighting the imbalance in political power.\n\nPolitically, Enlightenment ideas spread throughout France, promoting concepts"
   ]
  },
  {
   "decode_tps": 181.11760884507964,
   "prefill_tps": 3086.5361251455815,
   "free_mb": 1034,
   "texts": [
    "A **hash map** (also known as a **hash table**) is a data structure that stores key-value pairs and uses a **hash function** to compute an index (or \"bucket\") where the key-value pair should be stored. However, since the hash function maps keys to a finite number of indices, **collisions**\u2014where two different keys hash to the same index\u2014are inevitable.\n\nTo handle collisions, hash maps use various strategies. The two most common methods are:\n\n---\n\n### 1. **Chaining (Separate Chaining)**\n\n- **How it works**:  \n  Each bucket (index) in the hash map is a **linked list** (or another data structure like a tree or array) that stores all key-value pairs that hash to that index.\n\n- **Process**:\n  1. When inserting a key-value pair, the hash function computes the bucket index.\n  2. If the bucket is empty,",
    "Here's a Python class implementing a **Least Recently Used (LRU) Cache** with `get` and `put` methods:\n\n```python\nclass LRUCache:\n    def __init__(self, capacity: int):\n        \"\"\"\n        Initialize the LRU cache with a given capacity.\n        \n        Args:\n            capacity (int): Maximum number of items the cache can hold\n        \"\"\"\n        self.capacity = capacity\n        self.cache = {}  # Dictionary to store key-value pairs\n        self.order = []  # List to maintain access order (most recent at end)\n\n    def get(self, key: int) -> int:\n        \"\"\"\n        Retrieve a value from the cache by key.\n        \n        Args:\n            key (int): The key to look up\n            \n        Returns:\n            int: The value if key exists, otherwise -1\n        \"\"\"\n        if key in self.cache:\n            # Move the accessed key to the end (most",
    "The French Revolution was driven by a combination of long-standing social, economic, and political tensions. France was deeply divided by a rigid social structure known as the Ancien R\u00e9gime, which divided society into three estates: the clergy, the nobility, and the common people (the Third Estate). The Third Estate, which included peasants, workers, and the bourgeoisie, bore the brunt of taxation while having little political power, fueling widespread resentment.\n\nEconomically, France faced a severe financial crisis due to years of costly wars, including support for the American Revolution, extravagant spending by King Louis XVI, and an inefficient tax system. The government was unable to balance its budget, leading to massive debt. Attempts to reform taxation, such as calling the Estates-General in 1789, failed to resolve the crisis, as the Third Estate demanded equal representation, highlighting the imbalance in political power.\n\nPolitically, Enlightenment ideas spread throughout France, promoting concepts"
   ]
  },
  {
   "decode_tps": 178.5617409769745,
   "prefill_tps": 3089.4191325934244,
   "free_mb": 1035,
   "texts": [
    "A **hash map** (also known as a **hash table**) is a data structure that stores key-value pairs and uses a **hash function** to compute an index (or \"bucket\") where the key-value pair should be stored. However, since the hash function maps keys to a finite number of indices, **collisions**\u2014where two different keys hash to the same index\u2014are inevitable.\n\nTo handle collisions, hash maps use various strategies. The two most common methods are:\n\n---\n\n### 1. **Chaining (Separate Chaining)**\n\n- **How it works**:  \n  Each bucket (index) in the hash map is a **linked list** (or another data structure like a tree or array) that stores all key-value pairs that hash to that index.\n\n- **Process**:\n  1. When inserting a key-value pair, the hash function computes the bucket index.\n  2. If the bucket is empty,",
    "Here's a Python class implementing a **Least Recently Used (LRU) Cache** with `get` and `put` methods:\n\n```python\nclass LRUCache:\n    def __init__(self, capacity: int):\n        \"\"\"\n        Initialize the LRU cache with a given capacity.\n        \n        Args:\n            capacity (int): Maximum number of items the cache can hold\n        \"\"\"\n        self.capacity = capacity\n        self.cache = {}  # Dictionary to store key-value pairs\n        self.order = []  # List to maintain access order (most recent at end)\n\n    def get(self, key: int) -> int:\n        \"\"\"\n        Retrieve a value from the cache by key.\n        \n        Args:\n            key (int): The key to look up\n            \n        Returns:\n            int: The value if key exists, otherwise -1\n        \"\"\"\n        if key in self.cache:\n            # Move the accessed key to the end (most",
    "The French Revolution was driven by a combination of long-standing social, economic, and political tensions. France was deeply divided by a rigid social structure known as the Ancien R\u00e9gime, which divided society into three estates: the clergy, the nobility, and the common people (the Third Estate). The Third Estate, which included peasants, workers, and the bourgeoisie, bore the brunt of taxation while having little political power, fueling widespread resentment.\n\nEconomically, France faced a severe financial crisis due to years of costly wars, including support for the American Revolution, extravagant spending by King Louis XVI, and an inefficient tax system. The government was unable to balance its budget, leading to massive debt. Attempts to reform taxation, such as calling the Estates-General in 1789, failed to resolve the crisis, as the Third Estate demanded equal representation, highlighting the imbalance in political power.\n\nPolitically, Enlightenment ideas spread throughout France, promoting concepts"
   ]
  }
 ],
 "pinned": [
  {
   "decode_tps": 171.994155781915,
   "prefill_tps": 2984.364240288411,
   "free_mb": 1029,
   "texts": [
    "A **hash map** (also known as a **hash table**) is a data structure that stores key-value pairs and uses a **hash function** to compute an index (or \"bucket\") where the key-value pair should be stored. However, since the hash function maps keys to a finite number of indices, **collisions**\u2014where two different keys hash to the same index\u2014are inevitable.\n\nTo handle collisions, hash maps use various strategies. The two most common methods are:\n\n---\n\n### 1. **Chaining (Separate Chaining)**\n\n- **How it works**:  \n  Each bucket (index) in the hash map is a **linked list** (or another data structure like a tree or array) that stores all key-value pairs that hash to that index.\n\n- **Process**:\n  1. When inserting a key-value pair, the hash function computes the bucket index.\n  2. If the bucket is empty,",
    "Here's a Python class implementing a **Least Recently Used (LRU) Cache** with `get` and `put` methods:\n\n```python\nclass LRUCache:\n    def __init__(self, capacity: int):\n        \"\"\"\n        Initialize the LRU cache with a given capacity.\n        \n        Args:\n            capacity (int): Maximum number of items the cache can hold\n        \"\"\"\n        self.capacity = capacity\n        self.cache = {}  # Dictionary to store key-value pairs\n        self.order = []  # List to maintain access order (most recent at end)\n\n    def get(self, key: int) -> int:\n        \"\"\"\n        Retrieve a value from the cache by key.\n        \n        Args:\n            key (int): The key to look up\n            \n        Returns:\n            int: The value if key exists, otherwise -1\n        \"\"\"\n        if key in self.cache:\n            # Move the accessed key to the end (most",
    "The French Revolution was driven by a combination of long-standing social, economic, and political tensions. France was deeply divided by a rigid social structure known as the Ancien R\u00e9gime, which divided society into three estates: the clergy, the nobility, and the common people (the Third Estate). The Third Estate, which included peasants, workers, and the bourgeoisie, bore the brunt of taxation while having little political power, fueling widespread resentment.\n\nEconomically, France faced a severe financial crisis due to years of costly wars, including support for the American Revolution, extravagant spending by King Louis XVI, and an inefficient tax system. The government was unable to balance its budget, leading to massive debt. Attempts to reform taxation, such as calling the Estates-General in 1789, failed to resolve the crisis, as the Third Estate demanded equal representation, highlighting the imbalance in political power.\n\nPolitically, Enlightenment ideas spread throughout France, promoting concepts"
   ]
  },
  {
   "decode_tps": 177.62219389312696,
   "prefill_tps": 3088.334541979133,
   "free_mb": 1033,
   "texts": [
    "A **hash map** (also known as a **hash table**) is a data structure that stores key-value pairs and uses a **hash function** to compute an index (or \"bucket\") where the key-value pair should be stored. However, since the hash function maps keys to a finite number of indices, **collisions**\u2014where two different keys hash to the same index\u2014are inevitable.\n\nTo handle collisions, hash maps use various strategies. The two most common methods are:\n\n---\n\n### 1. **Chaining (Separate Chaining)**\n\n- **How it works**:  \n  Each bucket (index) in the hash map is a **linked list** (or another data structure like a tree or array) that stores all key-value pairs that hash to that index.\n\n- **Process**:\n  1. When inserting a key-value pair, the hash function computes the bucket index.\n  2. If the bucket is empty,",
    "Here's a Python class implementing a **Least Recently Used (LRU) Cache** with `get` and `put` methods:\n\n```python\nclass LRUCache:\n    def __init__(self, capacity: int):\n        \"\"\"\n        Initialize the LRU cache with a given capacity.\n        \n        Args:\n            capacity (int): Maximum number of items the cache can hold\n        \"\"\"\n        self.capacity = capacity\n        self.cache = {}  # Dictionary to store key-value pairs\n        self.order = []  # List to maintain access order (most recent at end)\n\n    def get(self, key: int) -> int:\n        \"\"\"\n        Retrieve a value from the cache by key.\n        \n        Args:\n            key (int): The key to look up\n            \n        Returns:\n            int: The value if key exists, otherwise -1\n        \"\"\"\n        if key in self.cache:\n            # Move the accessed key to the end (most",
    "The French Revolution was driven by a combination of long-standing social, economic, and political tensions. France was deeply divided by a rigid social structure known as the Ancien R\u00e9gime, which divided society into three estates: the clergy, the nobility, and the common people (the Third Estate). The Third Estate, which included peasants, workers, and the bourgeoisie, bore the brunt of taxation while having little political power, fueling widespread resentment.\n\nEconomically, France faced a severe financial crisis due to years of costly wars, including support for the American Revolution, extravagant spending by King Louis XVI, and an inefficient tax system. The government was unable to balance its budget, leading to massive debt. Attempts to reform taxation, such as calling the Estates-General in 1789, failed to resolve the crisis, as the Third Estate demanded equal representation, highlighting the imbalance in political power.\n\nPolitically, Enlightenment ideas spread throughout France, promoting concepts"
   ]
  },
  {
   "decode_tps": 178.57308940745352,
   "prefill_tps": 3093.4468681868325,
   "free_mb": 1034,
   "texts": [
    "A **hash map** (also known as a **hash table**) is a data structure that stores key-value pairs and uses a **hash function** to compute an index (or \"bucket\") where the key-value pair should be stored. However, since the hash function maps keys to a finite number of indices, **collisions**\u2014where two different keys hash to the same index\u2014are inevitable.\n\nTo handle collisions, hash maps use various strategies. The two most common methods are:\n\n---\n\n### 1. **Chaining (Separate Chaining)**\n\n- **How it works**:  \n  Each bucket (index) in the hash map is a **linked list** (or another data structure like a tree or array) that stores all key-value pairs that hash to that index.\n\n- **Process**:\n  1. When inserting a key-value pair, the hash function computes the bucket index.\n  2. If the bucket is empty,",
    "Here's a Python class implementing a **Least Recently Used (LRU) Cache** with `get` and `put` methods:\n\n```python\nclass LRUCache:\n    def __init__(self, capacity: int):\n        \"\"\"\n        Initialize the LRU cache with a given capacity.\n        \n        Args:\n            capacity (int): Maximum number of items the cache can hold\n        \"\"\"\n        self.capacity = capacity\n        self.cache = {}  # Dictionary to store key-value pairs\n        self.order = []  # List to maintain access order (most recent at end)\n\n    def get(self, key: int) -> int:\n        \"\"\"\n        Retrieve a value from the cache by key.\n        \n        Args:\n            key (int): The key to look up\n            \n        Returns:\n            int: The value if key exists, otherwise -1\n        \"\"\"\n        if key in self.cache:\n            # Move the accessed key to the end (most",
    "The French Revolution was driven by a combination of long-standing social, economic, and political tensions. France was deeply divided by a rigid social structure known as the Ancien R\u00e9gime, which divided society into three estates: the clergy, the nobility, and the common people (the Third Estate). The Third Estate, which included peasants, workers, and the bourgeoisie, bore the brunt of taxation while having little political power, fueling widespread resentment.\n\nEconomically, France faced a severe financial crisis due to years of costly wars, including support for the American Revolution, extravagant spending by King Louis XVI, and an inefficient tax system. The government was unable to balance its budget, leading to massive debt. Attempts to reform taxation, such as calling the Estates-General in 1789, failed to resolve the crisis, as the Third Estate demanded equal representation, highlighting the imbalance in political power.\n\nPolitically, Enlightenment ideas spread throughout France, promoting concepts"
   ]
  }
 ],
 "ubatch 1024": [
  {
   "decode_tps": 172.80323142042758,
   "prefill_tps": 3934.0541983713792,
   "free_mb": 966,
   "texts": [
    "A **hash map** (also known as a **hash table**) is a data structure that stores key-value pairs and uses a **hash function** to compute an index (or \"bucket\") where the key-value pair should be stored. However, since the hash function maps keys to a finite number of indices, **collisions**\u2014where two different keys hash to the same index\u2014are inevitable.\n\nTo handle collisions, hash maps use various strategies. The two most common methods are:\n\n---\n\n### 1. **Chaining (Separate Chaining)**\n\n- **How it works**:  \n  Each bucket (index) in the hash map is a **linked list** (or another data structure like a tree or array) that stores all key-value pairs that hash to that index.\n\n- **Process**:\n  1. When inserting a key-value pair, the hash function computes the bucket index.\n  2. If the bucket is empty,",
    "Here's a Python class implementing a **Least Recently Used (LRU) Cache** with `get` and `put` methods:\n\n```python\nclass LRUCache:\n    def __init__(self, capacity: int):\n        \"\"\"\n        Initialize the LRU cache with a given capacity.\n        \n        Args:\n            capacity (int): Maximum number of items the cache can hold\n        \"\"\"\n        self.capacity = capacity\n        self.cache = {}  # Dictionary to store key-value pairs\n        self.order = []  # List to maintain access order (most recent at end)\n\n    def get(self, key: int) -> int:\n        \"\"\"\n        Retrieve a value from the cache by key.\n        \n        Args:\n            key (int): The key to look up\n            \n        Returns:\n            int: The value if key exists, otherwise -1\n        \"\"\"\n        if key in self.cache:\n            # Move the accessed key to the end (most",
    "The French Revolution was driven by a combination of long-standing social, economic, and political tensions. France was deeply divided by a rigid social structure known as the Ancien R\u00e9gime, which divided society into three estates: the clergy, the nobility, and the common people (the Third Estate). The Third Estate, which included peasants, workers, and the bourgeoisie, bore the brunt of taxation while having little political power, fueling widespread resentment.\n\nEconomically, France faced a severe financial crisis due to years of costly wars, including support for the American Revolution, extravagant spending by King Louis XVI, and an inefficient tax system. The government was unable to balance its budget, leading to massive debt. Attempts to reform taxation, such as calling the Estates-General in 1789, failed to resolve the crisis, as the Third Estate demanded equal representation, highlighting the imbalance in political power.\n\nPolitically, Enlightenment ideas spread throughout France, promoting concepts"
   ]
  },
  {
   "decode_tps": 176.61170445012434,
   "prefill_tps": 4069.5651213274487,
   "free_mb": 962,
   "texts": [
    "A **hash map** (also known as a **hash table**) is a data structure that stores key-value pairs and uses a **hash function** to compute an index (or \"bucket\") where the key-value pair should be stored. However, since the hash function maps keys to a finite number of indices, **collisions**\u2014where two different keys hash to the same index\u2014are inevitable.\n\nTo handle collisions, hash maps use various strategies. The two most common methods are:\n\n---\n\n### 1. **Chaining (Separate Chaining)**\n\n- **How it works**:  \n  Each bucket (index) in the hash map is a **linked list** (or another data structure like a tree or array) that stores all key-value pairs that hash to that index.\n\n- **Process**:\n  1. When inserting a key-value pair, the hash function computes the bucket index.\n  2. If the bucket is empty,",
    "Here's a Python class implementing a **Least Recently Used (LRU) Cache** with `get` and `put` methods:\n\n```python\nclass LRUCache:\n    def __init__(self, capacity: int):\n        \"\"\"\n        Initialize the LRU cache with a given capacity.\n        \n        Args:\n            capacity (int): Maximum number of items the cache can hold\n        \"\"\"\n        self.capacity = capacity\n        self.cache = {}  # Dictionary to store key-value pairs\n        self.order = []  # List to maintain access order (most recent at end)\n\n    def get(self, key: int) -> int:\n        \"\"\"\n        Retrieve a value from the cache by key.\n        \n        Args:\n            key (int): The key to look up\n            \n        Returns:\n            int: The value if key exists, otherwise -1\n        \"\"\"\n        if key in self.cache:\n            # Move the accessed key to the end (most",
    "The French Revolution was driven by a combination of long-standing social, economic, and political tensions. France was deeply divided by a rigid social structure known as the Ancien R\u00e9gime, which divided society into three estates: the clergy, the nobility, and the common people (the Third Estate). The Third Estate, which included peasants, workers, and the bourgeoisie, bore the brunt of taxation while having little political power, fueling widespread resentment.\n\nEconomically, France faced a severe financial crisis due to years of costly wars, including support for the American Revolution, extravagant spending by King Louis XVI, and an inefficient tax system. The government was unable to balance its budget, leading to massive debt. Attempts to reform taxation, such as calling the Estates-General in 1789, failed to resolve the crisis, as the Third Estate demanded equal representation, highlighting the imbalance in political power.\n\nPolitically, Enlightenment ideas spread throughout France, promoting concepts"
   ]
  },
  {
   "decode_tps": 179.8872331906936,
   "prefill_tps": 4060.9402861261083,
   "free_mb": 963,
   "texts": [
    "A **hash map** (also known as a **hash table**) is a data structure that stores key-value pairs and uses a **hash function** to compute an index (or \"bucket\") where the key-value pair should be stored. However, since the hash function maps keys to a finite number of indices, **collisions**\u2014where two different keys hash to the same index\u2014are inevitable.\n\nTo handle collisions, hash maps use various strategies. The two most common methods are:\n\n---\n\n### 1. **Chaining (Separate Chaining)**\n\n- **How it works**:  \n  Each bucket (index) in the hash map is a **linked list** (or another data structure like a tree or array) that stores all key-value pairs that hash to that index.\n\n- **Process**:\n  1. When inserting a key-value pair, the hash function computes the bucket index.\n  2. If the bucket is empty,",
    "Here's a Python class implementing a **Least Recently Used (LRU) Cache** with `get` and `put` methods:\n\n```python\nclass LRUCache:\n    def __init__(self, capacity: int):\n        \"\"\"\n        Initialize the LRU cache with a given capacity.\n        \n        Args:\n            capacity (int): Maximum number of items the cache can hold\n        \"\"\"\n        self.capacity = capacity\n        self.cache = {}  # Dictionary to store key-value pairs\n        self.order = []  # List to maintain access order (most recent at end)\n\n    def get(self, key: int) -> int:\n        \"\"\"\n        Retrieve a value from the cache by key.\n        \n        Args:\n            key (int): The key to look up\n            \n        Returns:\n            int: The value if key exists, otherwise -1\n        \"\"\"\n        if key in self.cache:\n            # Move the accessed key to the end (most",
    "The French Revolution was driven by a combination of long-standing social, economic, and political tensions. France was deeply divided by a rigid social structure known as the Ancien R\u00e9gime, which divided society into three estates: the clergy, the nobility, and the common people (the Third Estate). The Third Estate, which included peasants, workers, and the bourgeoisie, bore the brunt of taxation while having little political power, fueling widespread resentment.\n\nEconomically, France faced a severe financial crisis due to years of costly wars, including support for the American Revolution, extravagant spending by King Louis XVI, and an inefficient tax system. The government was unable to balance its budget, leading to massive debt. Attempts to reform taxation, such as calling the Estates-General in 1789, failed to resolve the crisis, as the Third Estate demanded equal representation, highlighting the imbalance in political power.\n\nPolitically, Enlightenment ideas spread throughout France, promoting concepts"
   ]
  }
 ],
 "pinned + ubatch 1024": [
  {
   "decode_tps": 175.84795501955853,
   "prefill_tps": 3995.6017197221654,
   "free_mb": 966,
   "texts": [
    "A **hash map** (also known as a **hash table**) is a data structure that stores key-value pairs and uses a **hash function** to compute an index (or \"bucket\") where the key-value pair should be stored. However, since the hash function maps keys to a finite number of indices, **collisions**\u2014where two different keys hash to the same index\u2014are inevitable.\n\nTo handle collisions, hash maps use various strategies. The two most common methods are:\n\n---\n\n### 1. **Chaining (Separate Chaining)**\n\n- **How it works**:  \n  Each bucket (index) in the hash map is a **linked list** (or another data structure like a tree or array) that stores all key-value pairs that hash to that index.\n\n- **Process**:\n  1. When inserting a key-value pair, the hash function computes the bucket index.\n  2. If the bucket is empty,",
    "Here's a Python class implementing a **Least Recently Used (LRU) Cache** with `get` and `put` methods:\n\n```python\nclass LRUCache:\n    def __init__(self, capacity: int):\n        \"\"\"\n        Initialize the LRU cache with a given capacity.\n        \n        Args:\n            capacity (int): Maximum number of items the cache can hold\n        \"\"\"\n        self.capacity = capacity\n        self.cache = {}  # Dictionary to store key-value pairs\n        self.order = []  # List to maintain access order (most recent at end)\n\n    def get(self, key: int) -> int:\n        \"\"\"\n        Retrieve a value from the cache by key.\n        \n        Args:\n            key (int): The key to look up\n            \n        Returns:\n            int: The value if key exists, otherwise -1\n        \"\"\"\n        if key in self.cache:\n            # Move the accessed key to the end (most",
    "The French Revolution was driven by a combination of long-standing social, economic, and political tensions. France was deeply divided by a rigid social structure known as the Ancien R\u00e9gime, which divided society into three estates: the clergy, the nobility, and the common people (the Third Estate). The Third Estate, which included peasants, workers, and the bourgeoisie, bore the brunt of taxation while having little political power, fueling widespread resentment.\n\nEconomically, France faced a severe financial crisis due to years of costly wars, including support for the American Revolution, extravagant spending by King Louis XVI, and an inefficient tax system. The government was unable to balance its budget, leading to massive debt. Attempts to reform taxation, such as calling the Estates-General in 1789, failed to resolve the crisis, as the Third Estate demanded equal representation, highlighting the imbalance in political power.\n\nPolitically, Enlightenment ideas spread throughout France, promoting concepts"
   ]
  },
  {
   "decode_tps": 179.55543197265024,
   "prefill_tps": 4064.1941325007274,
   "free_mb": 963,
   "texts": [
    "A **hash map** (also known as a **hash table**) is a data structure that stores key-value pairs and uses a **hash function** to compute an index (or \"bucket\") where the key-value pair should be stored. However, since the hash function maps keys to a finite number of indices, **collisions**\u2014where two different keys hash to the same index\u2014are inevitable.\n\nTo handle collisions, hash maps use various strategies. The two most common methods are:\n\n---\n\n### 1. **Chaining (Separate Chaining)**\n\n- **How it works**:  \n  Each bucket (index) in the hash map is a **linked list** (or another data structure like a tree or array) that stores all key-value pairs that hash to that index.\n\n- **Process**:\n  1. When inserting a key-value pair, the hash function computes the bucket index.\n  2. If the bucket is empty,",
    "Here's a Python class implementing a **Least Recently Used (LRU) Cache** with `get` and `put` methods:\n\n```python\nclass LRUCache:\n    def __init__(self, capacity: int):\n        \"\"\"\n        Initialize the LRU cache with a given capacity.\n        \n        Args:\n            capacity (int): Maximum number of items the cache can hold\n        \"\"\"\n        self.capacity = capacity\n        self.cache = {}  # Dictionary to store key-value pairs\n        self.order = []  # List to maintain access order (most recent at end)\n\n    def get(self, key: int) -> int:\n        \"\"\"\n        Retrieve a value from the cache by key.\n        \n        Args:\n            key (int): The key to look up\n            \n        Returns:\n            int: The value if key exists, otherwise -1\n        \"\"\"\n        if key in self.cache:\n            # Move the accessed key to the end (most",
    "The French Revolution was driven by a combination of long-standing social, economic, and political tensions. France was deeply divided by a rigid social structure known as the Ancien R\u00e9gime, which divided society into three estates: the clergy, the nobility, and the common people (the Third Estate). The Third Estate, which included peasants, workers, and the bourgeoisie, bore the brunt of taxation while having little political power, fueling widespread resentment.\n\nEconomically, France faced a severe financial crisis due to years of costly wars, including support for the American Revolution, extravagant spending by King Louis XVI, and an inefficient tax system. The government was unable to balance its budget, leading to massive debt. Attempts to reform taxation, such as calling the Estates-General in 1789, failed to resolve the crisis, as the Third Estate demanded equal representation, highlighting the imbalance in political power.\n\nPolitically, Enlightenment ideas spread throughout France, promoting concepts"
   ]
  },
  {
   "decode_tps": 174.19411030358162,
   "prefill_tps": 3905.002564024419,
   "free_mb": 963,
   "texts": [
    "A **hash map** (also known as a **hash table**) is a data structure that stores key-value pairs and uses a **hash function** to compute an index (or \"bucket\") where the key-value pair should be stored. However, since the hash function maps keys to a finite number of indices, **collisions**\u2014where two different keys hash to the same index\u2014are inevitable.\n\nTo handle collisions, hash maps use various strategies. The two most common methods are:\n\n---\n\n### 1. **Chaining (Separate Chaining)**\n\n- **How it works**:  \n  Each bucket (index) in the hash map is a **linked list** (or another data structure like a tree or array) that stores all key-value pairs that hash to that index.\n\n- **Process**:\n  1. When inserting a key-value pair, the hash function computes the bucket index.\n  2. If the bucket is empty,",
    "Here's a Python class implementing a **Least Recently Used (LRU) Cache** with `get` and `put` methods:\n\n```python\nclass LRUCache:\n    def __init__(self, capacity: int):\n        \"\"\"\n        Initialize the LRU cache with a given capacity.\n        \n        Args:\n            capacity (int): Maximum number of items the cache can hold\n        \"\"\"\n        self.capacity = capacity\n        self.cache = {}  # Dictionary to store key-value pairs\n        self.order = []  # List to maintain access order (most recent at end)\n\n    def get(self, key: int) -> int:\n        \"\"\"\n        Retrieve a value from the cache by key.\n        \n        Args:\n            key (int): The key to look up\n            \n        Returns:\n            int: The value if key exists, otherwise -1\n        \"\"\"\n        if key in self.cache:\n            # Move the accessed key to the end (most",
    "The French Revolution was driven by a combination of long-standing social, economic, and political tensions. France was deeply divided by a rigid social structure known as the Ancien R\u00e9gime, which divided society into three estates: the clergy, the nobility, and the common people (the Third Estate). The Third Estate, which included peasants, workers, and the bourgeoisie, bore the brunt of taxation while having little political power, fueling widespread resentment.\n\nEconomically, France faced a severe financial crisis due to years of costly wars, including support for the American Revolution, extravagant spending by King Louis XVI, and an inefficient tax system. The government was unable to balance its budget, leading to massive debt. Attempts to reform taxation, such as calling the Estates-General in 1789, failed to resolve the crisis, as the Third Estate demanded equal representation, highlighting the imbalance in political power.\n\nPolitically, Enlightenment ideas spread throughout France, promoting concepts"
   ]
  }
 ]
}