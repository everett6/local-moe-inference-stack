# Model weights -- too large for git, and not ours to redistribute
models/
Model Training/
*.gguf
*.safetensors

# Vendored upstream llama.cpp clone, kept locally for reading source only
llama.cpp-src/

# Runtime state / logs, regenerated on every run
state/
*.log

# Python
__pycache__/
*.pyc
.cache/

# Auto-refresh cycle's scratch output
models/refreshed/

# Captured target outputs for draft self-distillation -- regenerable, and large
data/draft_train_capture.jsonl

# Locally built llama-server variants (copies of LM Studio's backend with a swapped CPU library)
build/
